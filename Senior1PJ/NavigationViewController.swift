//
//  NavigationViewController.swift
//  Senior1PJ
//
//  Created by Krittamet Chuwongworaphinit on 28/6/2564 BE.
//

import UIKit

class NavigationViewController: UINavigationController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
//        tabBarItem.image = UIImage(named: "purse")
//        tabBarItem.image?.resizeImage(image: UIImage(named: "purse"), targetSize: CGSizeMake(50.0, 50.0))
//        let imagesize = resizeImage(image: UIImage(named: "stack")!, targetSize: CGSize(width: 25.0, height: 25.0))
//        
//        tabBarItem.image = imagesize
        
         

    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */
    
 

}

